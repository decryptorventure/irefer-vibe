// Re-export ui-kit Tooltip (single-component API: <Tooltip content={...}>{children}</Tooltip>)
// TooltipTrigger and TooltipContent are no longer available — use the single-component pattern.
export { Tooltip, TooltipProvider } from '@frontend-team/ui-kit';
